# Usage
```
rhy -config /data/rcache /remote/
```

refresh cache
```
rhy -a
```

refresh and print the state of x.py 
```
rhy -s ./x.py
```

waiting for change withing duration
```
rhy -s ./x.py 20s 
```